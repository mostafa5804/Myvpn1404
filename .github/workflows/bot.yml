name: V2Ray Collector Bot

on:
  schedule:
    - cron: '0 * * * *'   # دقیقه 0 (هر ساعت)
    - cron: '40 * * * *'  # دقیقه 40 (هر ساعت)
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    
    steps:
    - name: Checkout
      uses: actions/checkout@v3
      with:
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install deps
      run: pip install telethon jdatetime pytz

    - name: Run bot
      env:
        API_ID: ${{ secrets.API_ID }}
        API_HASH: ${{ secrets.API_HASH }}
        SESSION_STRING: ${{ secrets.SESSION_STRING }}
      run: python main.py

    - name: Commit and Push
      run: |
        git config --global user.name "VPN Bot"
        git config --global user.email "bot@github.com"
        
        # 1. دریافت آخرین تغییرات مخزن برای جلوگیری از ارور Push
        git pull origin main --rebase
        
        # 2. اضافه کردن فایل html و فایل دیتابیس (data.json)
        if [ -f "index.html" ]; then
          git add index.html data.json
          
          # بررسی اینکه آیا تغییری ایجاد شده یا نه
          git diff --quiet && git diff --staged --quiet || (git commit -m "🔄 Update $(date +'%Y-%m-%d %H:%M')" && git push)
          echo "✅ Changes pushed successfully"
        else
          echo "⚠️ No index.html found"
        fi
