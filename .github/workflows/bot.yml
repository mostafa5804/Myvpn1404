name: Dual Session VPN Bot

on:
  schedule:
    - cron: '0 * * * *'   # هر ساعت سر دقیقه 00
    - cron: '30 * * * *'  # هر ساعت سر دقیقه 30
  workflow_dispatch:      # قابلیت اجرای دستی

permissions:
  contents: write

jobs:
  run-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 20  # افزایش تایم‌اوت بخاطر تست‌های دقیق‌تر
    
    steps:
    - name: Checkout
      uses: actions/checkout@v3
      with:
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install deps
      # اضافه شدن requests برای GeoIP
      run: pip install telethon jdatetime pytz requests

    - name: Pull latest changes
      run: |
        git config --global user.name "VPN Bot"
        git config --global user.email "bot@github.com"
        git pull origin main --rebase || echo "⚠️ Nothing to pull"

    - name: Run bot
      env:
        API_ID: ${{ secrets.API_ID }}
        API_HASH: ${{ secrets.API_HASH }}
        SESSION_STRING: ${{ secrets.SESSION_STRING }}
        SESSION_STRING_2: ${{ secrets.SESSION_STRING_2 }}
      run: python main.py

    - name: Check & Push Changes
      run: |
        git config --global user.name "VPN Bot"
        git config --global user.email "bot@github.com"
        
        # بررسی وجود فایل‌های حیاتی
        if [ ! -f "index.html" ]; then
          echo "❌ Error: index.html was not generated!"
          exit 1
        fi

        # افزودن همه فایل‌های تولید شده (شامل اشتراک و PWA)
        git add index.html data.json sub.txt manifest.json sw.js
        
        if git diff --staged --quiet; then
          echo "⚠️ No changes detected. Skipping push."
        else
          git commit -m "🔄 Auto-Update: $(date +'%Y-%m-%d %H:%M') [GeoIP + Sub]"
          git pull --rebase origin main
          git push origin HEAD:main
          echo "✅ Changes pushed successfully."
        fi
