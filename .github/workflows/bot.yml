name: Dual Session VPN Bot

on:
  schedule:
    - cron: '0 * * * *'   # هر ساعت سر دقیقه 00
    - cron: '30 * * * *'  # هر ساعت سر دقیقه 30
  workflow_dispatch:      # قابلیت اجرای دستی

permissions:
  contents: write

jobs:
  run-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    
    steps:
    - name: Checkout
      uses: actions/checkout@v3
      with:
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install deps
      run: pip install telethon jdatetime pytz

    - name: Pull latest changes
      # جلوگیری از تداخل نسخه‌ها قبل از اجرا
      run: |
        git config --global user.name "VPN Bot"
        git config --global user.email "bot@github.com"
        git pull origin main --rebase || echo "⚠️ Nothing to pull"

    - name: Run bot
      env:
        API_ID: ${{ secrets.API_ID }}
        API_HASH: ${{ secrets.API_HASH }}
        SESSION_STRING: ${{ secrets.SESSION_STRING }}
        SESSION_STRING_2: ${{ secrets.SESSION_STRING_2 }}
      run: python main.py

    - name: Check & Push Changes
      run: |
        # 1. تنظیم هویت
        git config --global user.name "VPN Bot"
        git config --global user.email "bot@github.com"
        
        # 2. بررسی وجود فایل خروجی
        if [ ! -f "index.html" ]; then
          echo "❌ Error: index.html was not generated!"
          exit 1
        fi

        # 3. افزودن فایل‌ها
        git add index.html data.json
        
        # 4. بررسی تغییرات
        if git diff --staged --quiet; then
          echo "⚠️ No changes detected. Skipping push."
        else
          git commit -m "🔄 Auto-Update: $(date +'%Y-%m-%d %H:%M')"
          
          # 5. دریافت آپدیت‌های جدید سرور (جلوگیری از ارور fetch first)
          git pull --rebase origin main
          
          # 6. آپلود نهایی
          git push origin HEAD:main
          echo "✅ Changes pushed successfully."
        fi
